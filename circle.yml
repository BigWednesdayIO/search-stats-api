machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t search-stats-api .

test:
  override:
    - docker run -w "/src" search-stats-api /bin/bash -c "grunt ci";
